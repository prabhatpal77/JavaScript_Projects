/*
window.innerWidth; // to access the width of the browser
window.innerHeight; // to access the height of the browser
window.open(); // to open a new tab
window.document; // to access the document sobject

// To access the complete body element
var body = document.body;
// To access the title of the current webpage
var title = document.title;
// to access the URL of the current webpage
var url = document.URL;

//DOM Methods/Functions
// To select all the HTML element with the specific ID (main-nav)
document.getElementById('main-nav');
// To select all the HTML elements with the class name (container)
document.getElementsByClassName('container');
// To select all the HTML elements using the HTML tag(img)
document.getElementsByTagName('img');
// To select the first element matching the selector (.main-nav a)
document.querySelector('.main-nav a');
// to select all the HTML elements matching the selector(.container p)
document.querySelectorAll('.container p'); 
*/